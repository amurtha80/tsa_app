# tsa_app
RShiny Mobile App TSA Historical Wait Times

---

## TODO

### High Priority — Post-Launch Fixes

#### Pi Cutover — Master Checklist (updated 2026-08-15)
Full cutover-to-Pi / desktop-to-backup-only migration status. See
`project_pi_nas_migration` memory and CHANGELOG.md (2026-08-13 through
2026-08-15 entries) for what's already been done — not restated here.

**Outstanding, in rough order:**
1. Observe several days of stable Pi-only operation (all 5 systemd jobs)
   before fully trusting it.
2. Stop desktop's `tsa_app_scraper` (the parallel comparison run) — wait for
   #1 first.
3. Once desktop's scraper is actually stopped (#2), enable
   `tsa_app_backup_pull_from_pi` (GUI or `Enable-ScheduledTask` from an
   elevated window — this tool's shell can't do it, see gotchas item 21).
   Trigger/logon-type already switched to At-startup/Password via GUI
   2026-08-15, task left disabled pending #2 — verified via raw XML
   (`<BootTrigger />`, `<LogonType>Password</LogonType>`,
   `<Enabled>false</Enabled>`, Command/Arguments unchanged/clean).
4. Once desktop is backup-only, decide its final footprint (likely just the
   backup-pull task).
5. Update `DEVELOPMENT.md` to reflect Pi-as-primary architecture once cutover
   is final (per CLAUDE.md, prompt for review before editing).

**Separate, unrelated pre-existing tech debt (not part of cutover):**
`renv.lock` drift on the Pi (R 4.5.1 in the lockfile vs R 4.6.1 installed,
~48 package version mismatches — real fix is a `renv::snapshot()` pass on
both machines, bigger decision, not urgent).

#### Outage — LAX `wait-times` Page Returning "Access Denied" (0 rows since 2026-08-07)
- 2026-08-08: LAX scraper (`LAX_wait_times.R`) returned 0 rows the day prior.
  Confirmed via `curl` that `https://www.flylax.com/wait-times` now returns
  HTTP 403 with a real Drupal "Access denied — you are not authorized to
  access this page" body (not a Cloudflare/bot block — the LAX homepage
  itself loads fine with the same client). The URL is also absent from
  flylax.com's own `sitemap.xml` (previously not checked/needed). User
  independently confirmed "not authorized" visiting the page manually on
  phone, ruling out a scraper-side/bot-detection issue. No replacement URL
  found on the homepage or sitemap. Scraper code is not at fault — it will
  keep failing until LAX republishes the page (at this URL or elsewhere).
  Action: periodically recheck `https://www.flylax.com/wait-times` for
  200/table-restored; if LAX moves the page, update `page_url` in
  `LAX_wait_times.R` accordingly.

### Medium Priority — Post-Launch Fixes

#### Deployment
- Add third chart for CLEAR lane wait times for airports that have CLEAR data (MIA; others TBD) — chart should only render when data exists for the selected airport/checkpoint combination

#### Data Quality — SLC Checkpoint Hours Need to Be Populated in the Database
- 2026-07-22: SLC shipped 2026-07-22 with its single-airport-wide-reading judgment call
  (see `SLC_wait_times.R`) but no `airport_checkpoint_hours` backstop rows were ever
  added. Needs the same "harvest `SELECT DISTINCT checkpoint` from live data,
  cross-reference the airport's own hours page" treatment already applied to PHL/MCO/etc.

#### Data Quality — DTW Checkpoint Hours Need to Be Populated in the Database
- 2026-07-22: DTW scraper (`DTW_wait_times.R`) shipped with no live open/closed signal
  in its payload and no `airport_checkpoint_hours` backstop rows. Once live for long
  enough, harvest the exact checkpoint name strings it writes (`Evans` / `McNamara`)
  and set hours from metroairport.com's own posted hours — same deferred pattern used
  for BOS's hours-monitor approach.

### New Airports (Lower Priority — Post MVP)
- **BWI** - `bwiairport.com/`; https://bwiairport.com/wp-content/themes/bwitheme/ajaxcall.php?action=get_waitTimes_data
- **CVG** - `cvgairport.com` Zensors
- **PIT** - `flypittsburg.com` Zensors
- **YYZ** - `https://www.torontopearson.com/en` Zensors

### Longer Term Updates | Fixes | Enhancements

#### Refactor Remaining `httr`-Based Scraper to `httr2`
- 2026-07-22: All current scrapers use `httr2` except `DEN_wait_times.R`, which still
  uses the legacy `httr` package (confirmed via project-wide grep). Low priority —
  refactor to `httr2` for consistency once other higher-priority work is clear.

#### Data Source — Investigate TSA's Own Android/Mobile Wait-Times App as a Backup Feed
- TSA's own Android app publishes checkpoint wait time data on its site, discovered while
  researching LAX (2026-07-11) — worth scoping as a fallback data source for airports where
  the airport's own site only has partial coverage (e.g. LAX only publishing TBIT)
- Granularity/quality looked worse than what individual airport sites (or this app) already
  provide, so this is a backup source, not a primary one — don't replace any existing scraper
  with it, just evaluate whether it's usable as a fallback when an airport site has gaps
- Scoping first: confirm what data/endpoint backs it, what airports/checkpoints it covers,
  and whether it's scrapable before committing to build anything against it
  
#### No-Coverage Airport Banner — ORD, TPA, and Future "No Live Data" Airports (Low Priority)
- 2026-07-16: LAX's `airport_coverage_notes` banner (see CHANGELOG 2026-07-16 "App —
  LAX Partial-Coverage UI Banner") currently handles *partial* coverage (an airport has
  some scraped checkpoints, but not all). ORD and TPA are a different case: no scraper
  exists for them at all, so they don't appear in the Airport dropdown today
  (`airport_choices` is derived from `sort(unique(summ_data$airport))` in app.R, which
  only lists airports with existing rows in the summary parquet).
- Before building this out: decide (a) whether to decouple the dropdown from
  `summ_data` so no-data airports can be selected with an empty-chart + warning-banner
  state, and (b) whether to give this a visually distinct "warning" tier separate from
  LAX's "info" styling — both were explicitly punted on 2026-07-16 as lower priority,
  not urgent.
- Related to the existing "TSA's Own Android/Mobile Wait-Times App as a Backup Feed"
  item below — if that fallback source ever gets wired in for ORD/TPA, this banner
  would need to flip off (or reword) for those airports rather than staying permanent.

#### LGA Terminal A: Monitor for Reactivation (Spirit Airlines Shutdown)
- 2026-07-13: Spirit Airlines ceased all operations at 3:00 AM ET on 2026-05-02,
  vacating LGA's Marine Air Terminal (Terminal A). Marked `is_active = FALSE` in
  `airport_checkpoint_hours` — app now shows "This checkpoint is not currently in
  use." for both lanes instead of real wait-time data (see CHANGELOG 2026-07-13 (2)).
  No firm reopening date exists as of this writing. When Terminal A resumes service
  (new tenant airline or Spirit's gates reassigned), insert one new
  `airport_checkpoint_hours` row for LGA Terminal A with `is_active = TRUE` — no code
  change needed, just a data row per the append-only convention.

### Housekeeping
- Update and format README for GitHub
- Standardize scraping function naming convention