# tsa_app
RShiny Mobile App TSA Historical Wait Times

---

## TODO

### High Priority — Post-Launch Fixes

#### Data Quality — EWR: Add `airport_checkpoint_hours` Rows for the 3 Split Terminal B Checkpoints
- 2026-07-13: `tsa_wait_times` EWR Terminal B was backfilled and the scraper fixed to
  capture 3 real checkpoints (`Terminal B 40-49`, `Terminal B 51-57`, `Terminal B
  60-68`) instead of one collapsed "Terminal B" row — see CHANGELOG 2026-07-13 (1).
  `airport_checkpoint_hours` still only has a single "Terminal B" row. Since
  `xx_build_summary_DB.R` joins on exact checkpoint name, the 3 new checkpoints won't
  match it and get no hours-based filtering (unmatched lookup = treated as
  unrestricted) until 3 corresponding rows are added.

#### Data Quality — EWR General Hours Still Unverified (No Official Source Found)
- Confirmed 2026-07-13: newarkairport.com's own Terminal A and Terminal C pages
  publish no operating hours at all (live wait times only); Terminal B's page has one
  hours block, which turned out to be CLEAR hours for gates 40-49 specifically, not
  general checkpoint hours (mislabeled as general in the existing
  `04_Assets/tsa_checkpoint_hours.html`/DB values). Current EWR general hours in
  `airport_checkpoint_hours` remain unverified secondary-source estimates.

#### Data Quality — EWR PreCheck/Hours Judgment Calls Need Recheck (by 2026-08-13)
- 2026-07-13: PreCheck close time was set to 8pm local for all 3 EWR checkpoints per
  explicit user instruction (decisive, not itself in question), but the underlying
  general hours it's paired with are unverified secondary-source estimates (see the
  "EWR General Hours Still Unverified" item above), and the newly split Terminal B
  checkpoints (`Terminal B 40-49`/`51-57`/`60-68`) have no `airport_checkpoint_hours`
  rows yet at all (see the item above). Revisit all three once ~1 month of fresh
  `tsa_wait_times` data has accumulated under the fixed scraper (by 2026-08-13) —
  same pattern as the JFK/LGA rechecks below.

#### Data Quality — JFK Terminal 1 24-Hour PreCheck Assumption Needs Recheck (by 2026-08-12)
- 2026-07-12: TSA's schedule tool shows JFK Terminal 1 PreCheck closing twice overnight
  (1-4am, 6-8am), but the checkpoint's own general hours (04:00-23:00, from
  `04_Assets/tsa_checkpoint_hours.html`) don't cover the tool's claimed open windows in
  that gap either — an unresolved conflict. Set to 24hr PreCheck as a judgment call
  (terminals generally don't cycle open/closed multiple times a day) rather than
  trusting either source outright. Revisit using ~1 month of fresh `tsa_wait_times`
  scraping data (by 2026-08-12) to make a final decision — a same-length lookback done
  2026-07-12 was inconclusive (see CHANGELOG 2026-07-12 (6) for why)

#### Data Quality — LGA PreCheck Edge Cases Need Recheck (by 2026-08-12)
- 2026-07-12: LGA PreCheck hours set to a 4am-8pm default across all 3 terminals per
  project convention, except Terminal B, which TSA's schedule tool showed opening at
  3am instead of 4am. Terminal A never showed a dedicated PreCheck listing anywhere in
  the tool's full 24-hour sweep (despite having general hours) — PreCheck/CLEAR hours
  for Terminal A were set to the 4am-8pm default as a judgment call, not a verified
  finding. Revisit both the Terminal B early-open and the Terminal A gap using ~1
  month of fresh `tsa_wait_times` scraping data (by 2026-08-12)

#### Data Quality — Checkpoint Name Cleanup
- **CLT checkpoint rename** — historical rows (Nov 2024 – Apr 2025) store `"Checkpoint 1"` for what is physically Checkpoint 2; new scraper (Jul 2026) writes correct names going forward; once sufficient fresh data has accumulated, run `UPDATE tsa_wait_times SET checkpoint = 'Checkpoint 2' WHERE airport = 'CLT' AND checkpoint = 'Checkpoint 1'` to canonicalize historical data, then rebuild parquet

#### Deployment
- Add third chart for CLEAR lane wait times for airports that have CLEAR data (MIA; others TBD) — chart should only render when data exists for the selected airport/checkpoint combination
- **LAX coverage note in app UI** — flylax.com/wait-times only publishes wait times for TBIT (the International Terminal), even though LAX physically has its own security checkpoint at nearly every terminal (1–8, with T8 sharing T7's). When a user selects LAX in the app, show a message stating that only TBIT wait times are currently available, so it's not misread as "LAX has one checkpoint total"

### Active Script Bugs
- **WebSocket warning** — suppress `"got non-close frame while closing"` warning during chromote teardown across all chromote scripts

### New Airports (Lower Priority — Post MVP)
- **BOS** — `BOS_wait_times.R` drafted; needs test run to confirm Zensors API Origin allowlist check passes from R; fallback is chromote approach if 403 persists
- **SLC** — `slcairport.com`; JS-rendered widget, investigate for API endpoint before falling back to chromote
- **DTW** — `metroairport.com`; Angular/Vue app, likely REST API behind it; inspect via DevTools before building script

### Longer Term Updates | Fixes | Enhancements

#### Data Source — Investigate TSA's Own Android/Mobile Wait-Times App as a Backup Feed
- TSA's own Android app publishes checkpoint wait time data on its site, discovered while
  researching LAX (2026-07-11) — worth scoping as a fallback data source for airports where
  the airport's own site only has partial coverage (e.g. LAX only publishing TBIT)
- Granularity/quality looked worse than what individual airport sites (or this app) already
  provide, so this is a backup source, not a primary one — don't replace any existing scraper
  with it, just evaluate whether it's usable as a fallback when an airport site has gaps
- Scoping first: confirm what data/endpoint backs it, what airports/checkpoints it covers,
  and whether it's scrapable before committing to build anything against it
  
#### LGA Terminal A: Monitor for Reactivation (Spirit Airlines Shutdown)
- 2026-07-13: Spirit Airlines ceased all operations at 3:00 AM ET on 2026-05-02,
  vacating LGA's Marine Air Terminal (Terminal A). Marked `is_active = FALSE` in
  `airport_checkpoint_hours` — app now shows "This checkpoint is not currently in
  use." for both lanes instead of real wait-time data (see CHANGELOG 2026-07-13 (2)).
  No firm reopening date exists as of this writing. When Terminal A resumes service
  (new tenant airline or Spirit's gates reassigned), insert one new
  `airport_checkpoint_hours` row for LGA Terminal A with `is_active = TRUE` — no code
  change needed, just a data row per the append-only convention.

### Housekeeping
- Update and format README for GitHub
- Standardize scraping function naming convention
- Remove stale `# TODO: replace bucket name` comment from `xx_build_summary_DB.R` line 109